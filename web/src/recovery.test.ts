import { RecoveryTracker } from './recovery'; import type { PodView } from './types';
const pod=(uid:string,ready=false):PodView=>({uid,name:uid,namespace:'payments',node:'node-1',phase:'Running',ready,restartCount:0,reason:'',cpuMillis:10,memMib:16,createdAt:'2026-09-10T12:00:00.000Z',deletionTimestamp:'',controller:{apiVersion:'apps/v1',kind:'ReplicaSet',name:'api',uid:'rs-1'}});
export function runRecoveryTests(){let now=100;const t=new RecoveryTracker(()=>now),target=pod('old'),sibling=pod('existing',true),op=t.begin(target,[target,sibling]);t.accepted(op.id);if(op.phase!=='accepted')throw Error('acceptance');t.observe({type:'updated',pod:sibling});now=200;t.observe({type:'deleted',uid:'old'});now=300;t.observe({type:'added',pod:pod('new')});now=1200;t.observe({type:'updated',pod:pod('new',true)});if(String(op.phase)!=='ready'||op.readyObservedAt!-op.startedAt!==1100)throw Error('recovery')}
